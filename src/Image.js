class Image {


  constructor(imageObj) {
    this.id = imageObj.id
    this.url = imageObj.url
    this.name = imageObj.name
    this.like_count = imageObj.like_count
    this.uuid = imageObj.uuid
    this.comments = imageObj.comments

  }

}

// Not Used
